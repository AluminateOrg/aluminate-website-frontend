name: Website Frontend CD

on:
  push:
    branches: [ production ]

permissions:
  contents: read

concurrency:
  group: production-website-frontend-cd
  cancel-in-progress: true

jobs:
  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Build Docker Image
        env:
          NEXT_PUBLIC_BACKEND_URL: ${{ secrets.NEXT_PUBLIC_BACKEND_URL }}
          NEXT_PUBLIC_API_PREFIX: ${{ secrets.NEXT_PUBLIC_API_PREFIX }}
          NEXT_PUBLIC_GLOBAL_PUBLIC_KEY: ${{ secrets.NEXT_PUBLIC_GLOBAL_PUBLIC_KEY }}
          NEXT_PUBLIC_PAYHERE_MERCHANT_ID: ${{ secrets.NEXT_PUBLIC_PAYHERE_MERCHANT_ID }}
          WEBSITE_FRONTEND_PORT: ${{ secrets.WEBSITE_FRONTEND_PORT }}
        run: |
          docker build --pull \
            --build-arg NEXT_PUBLIC_BACKEND_URL="$NEXT_PUBLIC_BACKEND_URL" \
            --build-arg NEXT_PUBLIC_API_PREFIX="$NEXT_PUBLIC_API_PREFIX" \
            --build-arg NEXT_PUBLIC_GLOBAL_PUBLIC_KEY="$NEXT_PUBLIC_GLOBAL_PUBLIC_KEY" \
            --build-arg NEXT_PUBLIC_PAYHERE_MERCHANT_ID="$NEXT_PUBLIC_PAYHERE_MERCHANT_ID" \
            --build-arg WEBSITE_FRONTEND_PORT="$WEBSITE_FRONTEND_PORT" \
            -t aluminate/website-frontend:${{ github.sha }} \
            -t aluminate/website-frontend:latest \
            .

      - name: Save Docker Image
        run: docker save aluminate/website-frontend:latest | gzip > website-frontend.tar.gz

      - name: Copy Image to Droplet
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.DROPLET_HOST }}
          username: ${{ secrets.DROPLET_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: website-frontend.tar.gz
          target: /opt/aluminate/app/aluminate-global-backend/
          rm: false

      - name: Load & Restart on Droplet
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DROPLET_HOST }}
          username: ${{ secrets.DROPLET_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -euo pipefail

            echo "Loading new Docker image..."
            cd /opt/aluminate/app/aluminate-global-backend
            docker load < website-frontend.tar.gz
            rm -f website-frontend.tar.gz

            echo "Recreating website-frontend container..."
            docker compose -f /opt/aluminate/app/aluminate-global-backend/docker-compose.yml \
              up -d --force-recreate --no-deps website-frontend

            echo "Cleaning unused images..."
            docker image prune -f

            echo "Waiting for startup..."
            sleep 15

            echo "Container status:"
            docker ps | grep aluminate-global-backend-website-frontend

            echo "Recent logs:"
            docker compose -f /opt/aluminate/app/aluminate-global-backend/docker-compose.yml \
              logs --tail=50 website-frontend

            echo "Deployment complete"

      - name: Deployment Success
        run: echo "Website Frontend deployed to Production"